<h1>Ingresar Prueba</h1>
<%= form_for([@pump, test]) do |form| %>
  <% if test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pump.errors.count, "error") %> prohibited this forum_thread from being saved:</h2>

      <ul>
      <% test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Diametro_rodete %>
    <%= form.text_area :diametro_rodete, id: :test_diametro_rodete %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<h1>Importar Prueba</h1>
<%= form_tag import_pump_tests_path(@pump), multipart: true do %>
  <%= file_field_tag :file %>
  <%= submit_tag "Importar" %>
<% end %>